# mutation-serial-suites.tsv — paired suites that cannot tolerate a concurrent SIBLING.
#
# TAB-separated. Columns:
#   selftest <TAB> reason
#
# The sweep scores mutants in a worker pool, and two mutants of the same guard run the same
# paired suite AT THE SAME TIME. A suite that owns a fixed resource — a literal path outside
# its own mktemp tree, a fixed port, a well-known lock — corrupts its sibling when that
# happens, and the corruption reads as a mutation verdict rather than as a harness fault.
#
# A row here pins every guard whose kill set contains the suite into the sweep's SERIAL pass:
# those mutants run one at a time, after the pool has drained. The cost is that guard's
# mutants x that suite's seconds, unshared — so a row is a real bill, not a free safety net.
#
# THE ALTERNATIVE IS ALWAYS BETTER WHEN IT IS AVAILABLE: fix the suite to use `mktemp` and
# delete its row. A row is for the case where the fix is out of the editing scope of the
# change that discovered the hazard, and it must say so rather than reading as a verdict that
# the suite is unfixable.
#
# ABSENT means parallel-safe. This file is a deny-list, not a registry: adding a suite is a
# claim about a hazard someone identified, and the reason column is where that claim lives.
#
# THE THREE ROWS BELOW ARE ONE DEFECT IN THREE PLACES: `bash "$CHECK" ... >/tmp/<fixed>.out`
# followed by `grep -q ... /tmp/<fixed>.out`. Pointing TMPDIR at a per-item scratch directory
# (which the sweep does) does NOT move them, because the paths are absolute literals. Two
# concurrent instances therefore interleave a write and a read of the same file, and the
# grep answers about the WRONG mutant — a false kill or a false survivor depending on which
# write landed last. All three fixes are a handful of lines in suites outside the tools/ +
# docs/ editing scope of the change that introduced the pool, so they are pinned with the
# reason rather than left to surface as flake.
#
# The rest of the audit that produced this list found no other hazard: no suite binds a
# fixed port; the four suites that call `git worktree add` all do so inside their own mktemp
# repo with per-repo names; and the only `pgrep` user is this harness's own companion suite,
# which is never a killer (its guard is excluded).
plugins/dev-pipeline/skills/run/tools/check-doc-routing-selftest.sh	Literal /tmp/docroute-moved.out and /tmp/docroute-deleted.out — written, then grepped.
plugins/dev-pipeline/skills/run/tools/check-extensions-selftest.sh	Literal /tmp/ext-selftest.out and /tmp/ext-ref.out — written, then grepped; ext-ref.out is reused across two cases within one run, so it races itself twice over.
plugins/dev-pipeline/skills/run/tools/check-config-shadowing-selftest.sh	Literal /tmp/shadow-selftest.out and /tmp/shadow-selftest2.out — written, then grepped.
