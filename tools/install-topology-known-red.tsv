# install-topology-known-red.tsv — shipped suites accepted as red under the version-keyed
# install topology that tools/install-topology-selftest.sh stages.
#
# A suite listed here is REPORT-ONLY. A suite NOT listed here that fails or times out is a red
# build. A listed suite that PASSES is a warning, never a red — that is the "shrink the list"
# direction, the same contract tools/mutation-baseline.tsv carries for survivors. A row that
# matches no staged suite is a stale-row warning, also never a red. So a fix and the removal of
# its row do not have to land in the same commit.
#
# TAB-separated. Columns:
#   repo-relative path <TAB> cause
#
# The key is the REPO-relative path (`plugins/<name>/<rel>`), NOT the staged one: a staged path
# carries a version segment, so keying on it would rot every row at the next release.
#
# EVERY ROW STATES ITS CAUSE. A row whose cause is not actually known reads `undiagnosed` —
# an admission, visibly, rather than an invented rationale that reads like an exemption.
# Draining this list is tracked in #421.
#
# SOME ROWS ARE ENVIRONMENT-DEPENDENT, and those say so in their cause. A suite whose failure
# depends on the bash version or on which CLIs are installed will PASS on some machines, and
# the guard will then warn "listed known-red but PASSED — drop its row". Do NOT act on that
# warning from a single green run: read the cause first, and drop the row only once the suite
# passes in the environment the cause names. The two rows added after the first CI run are
# exactly this kind, and they are why the seeding number below has two figures rather than one.
#
# Seeded from the guard's first run on the authoring machine — 55 suites, 51 pass, 4 listed —
# and CORRECTED by the first run in a second environment, which is the only kind that can
# falsify a "measured here" claim: CI scores 49 pass, 6 listed on both lanes. The two extra
# rows were real failures the authoring machine's environment hid. Two suites and
# design-sync-selftest.mjs were deliberately ABSENT — both were fixed rather than listed, and
# so was workflows-mjs-selftest.sh, which was red only because it invoked design-sync's.
# (design-sync-selftest.mjs itself was retired in #574 with its engine.)
#
# The three fixed-hop-count rows — check-emit-deadline-selftest.sh, doctor-selftest.sh and
# preflight-selftest.sh — are DRAINED: each site now runs the house sibling-resolution ladder,
# and all three pass from a staged cache. preflight-selftest.sh's was one of the two
# ENVIRONMENT-DEPENDENT rows above, so its removal is unverified in the authoring environment
# by construction; what the suite now asserts directly is that the ladder resolves, and a wrong
# call here warns rather than reds. The two repo-only-artifact rows were drained separately, by
# the sibling slice that gave those suites a counted skip.
#
# THE LIST IS NOW EMPTY, which is the state #421 called done. The last row was
# cost-block-selftest.sh, whose mirror of pipeline-cost-block.sh's state resolution anchored on
# $HERE while the script anchors on $PWD. It is not anchored differently now — it is not a
# mirror at all: the suite drives the script through STATECTL_STATE_DIR, the script's own
# highest-precedence override, which every other run in that file already used. Nothing is
# derived, so nothing can diverge.
#
# That row was the second ENVIRONMENT-DEPENDENT one, and its two halves are why a single green
# run was never evidence: on bash <= 5.2 `cd ""` is a silent no-op (measured on stock 3.2 from a
# staged cache: 51 passed, 3 failed), and on bash >= 5.3 it errors, STATE_DIR collapses to ./,
# and it coincidentally AGREES with the script (61 passed, 0 failed on the same tree). Keep this
# file's rows empty by fixing suites, not by listing them.
