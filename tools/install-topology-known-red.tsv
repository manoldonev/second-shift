# install-topology-known-red.tsv — shipped suites accepted as red under the version-keyed
# install topology that tools/install-topology-selftest.sh stages.
#
# A suite listed here is REPORT-ONLY. A suite NOT listed here that fails or times out is a red
# build. A listed suite that PASSES is a warning, never a red — that is the "shrink the list"
# direction, the same contract tools/mutation-baseline.tsv carries for survivors. A row that
# matches no staged suite is a stale-row warning, also never a red.
#
# TAB-separated. Columns:
#   repo-relative path <TAB> cause
#
# The key is the REPO-relative path (`plugins/<name>/<rel>`), NOT the staged one: a staged path
# carries a version segment, so keying on it would rot every row at the next release.
#
# EVERY ROW STATES ITS CAUSE. A row whose cause is not actually known reads `undiagnosed` —
# an admission, visibly, rather than an invented rationale that reads like an exemption. That
# is the flag half of #419's OR-1: draining this list is what closes it.
#
# plan-lint-selftest.sh and design-sync-selftest.mjs are deliberately ABSENT — #419 fixed both.
plugins/review-toolkit/scripts/check-review-context-sections-selftest.sh	needs docs/extension-points.md, which lives in this repo and is not shipped inside any plugin. Deferred to #420.
plugins/dev-pipeline/skills/run/tools/config-lint-selftest.sh	needs schema/second-shift.config.schema.json, which lives in this repo and is not shipped inside any plugin. Deferred to #420.
plugins/dev-pipeline/skills/run/tools/cost-block-selftest.sh	writes ./.claude/pipeline-state/... relative to cwd, so it depends on the consumer checkout it is run from rather than on a fixture. Deferred to #420.
