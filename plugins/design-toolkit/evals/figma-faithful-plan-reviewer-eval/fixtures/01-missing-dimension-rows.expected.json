{
  "expected_verdict": "block",
  "expected_findings": [
    "the `dimensions` table has a header and separator but ZERO rows, on a control-bearing screen (a NumberField, a Select, a Button, a DataTable) — step 3b requires sizing per node unconditionally, so every control will be sized by eye and the design-blind code reviewer downstream has no recorded number to compare against"
  ],
  "must_not_flag": [
    "the token table arithmetic — every row is correct on the 4px base (24px→6, 16px→4, 12px→3, 8px→2)",
    "the inter-block gap rows — the header→table and panel→sibling-notice rows are present, so step 3b was not skipped for spacing",
    "the placement decision — stated as a sibling AND the file list edits ShippingSettingsPage.tsx, so hierarchy and structure agree",
    "the component resolutions — each `why this component` cell describes what the frame draws, not what the layer is called",
    "the analog — TaxRulesPanel is a close structural match",
    "the Decision Ledger — two rows, both provenances inside the closed enum"
  ],
  "layer": "missing dimension rows",
  "notes": "Ticket fixture 1. This is the SILENT case the plan reviewer's own checklist calls out: everything else in the plan is correct, and the defect is an empty table rather than a wrong value. A reviewer that only reads filled cells will pass it. The #692 run is where this shipped: design called for a 32px control height, a 48px numeric input and a 96px unit select; the build shipped ~107px and ~172px, and the plan recorded no dimension at all."
}
