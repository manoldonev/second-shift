{
  "expected_verdict": "pass",
  "expected_findings": [],
  "must_not_flag": [
    "every token row — 24px→6, 16px→4, 12px→3, 8px→2 are all correct on the 4px base, and the inter-block rows use the same arithmetic",
    "the `dimensions` table — one row per sized node with an overflow decision each",
    "every `why this component` cell — each describes what the frame draws and, for the panel shell, names the prop that suppresses the catalog default",
    "the placement decision — stated as a sibling AND the file list edits NotificationSettingsPage.tsx, so hierarchy and structure agree",
    "the analog — SessionPreferencesPanel is a close structural match",
    "the Decision Ledger — two rows, provenances `codebase-derived` and `deferred`, both inside the closed enum",
    "the file list — screens, components, hooks and the registration edit are all covered"
  ],
  "layer": "clean control",
  "notes": "The calibration fixture. The agent's stated posture is 'Empty review is a valid output … do not invent findings to look thorough', and this plan gives it several near-miss opportunities: a `deferred` provenance that is legal but looks like an unanswered question, a switch control whose 36x20px dimensions are off the 4px spacing scale (dimensions are measurements, not spacing tokens — not a finding), a `Card` used with an override rather than avoided, and a 720px max that no token covers. A Blocker on any of them is a false positive."
}
