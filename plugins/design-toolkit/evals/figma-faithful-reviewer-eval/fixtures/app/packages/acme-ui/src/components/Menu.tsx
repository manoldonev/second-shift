// Export stub for the figma-faithful-reviewer eval — see ../../../../../design-tokens/acme-ui-catalog.md.
// The eval measures whether the reviewer VERIFIES a catalog component exists before flagging a
// hand-rolled substitute, so the export has to be greppable. No implementation is needed.
export const Menu = null;
