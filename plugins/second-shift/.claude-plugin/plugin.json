{
  "name": "second-shift",
  "description": "Marketplace onboarding + health: /second-shift:onboard writes a consumer repo's config, settings pin, and lockfile from provenance-first detection; /second-shift:doctor verifies install state against the lockfile. User-scope bootstrap plugin: zero agents, zero hooks, near-zero context cost.",
  "version": "1.2.0",
  "author": {
    "name": "Manol Donev"
  }
}
