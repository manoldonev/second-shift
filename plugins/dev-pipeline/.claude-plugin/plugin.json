{
  "name": "dev-pipeline",
  "description": "Autonomous ticket-to-PR pipeline across intake → build → review → merge-boundary blocks, gated by lean's five artifact milestones by default (/dev-pipeline:run-lean); the ten-stage statectl state machine (/dev-pipeline:run) is kept as an ablation/rollback lane. Deterministic verify runner (verifyctl), plan lint with AC traceability, tracker adapters (github|jira), Workflow-based review/plan/mutation gates, cost tracking, retro. Reads per-repo static context from .claude/second-shift.config.json.",
  "version": "3.8.2",
  "author": {
    "name": "Manol Donev"
  }
}
