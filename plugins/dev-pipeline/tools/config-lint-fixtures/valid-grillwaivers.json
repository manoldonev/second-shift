{
  "$schema": "https://raw.githubusercontent.com/manoldonev/second-shift/v2.1.0/schema/second-shift.config.schema.json",
  "configVersion": 2,
  "tracker": { "type": "github" },
  "topology": { "type": "standalone", "repos": { "app": { "path": ".", "baseBranch": "main" } } },
  "commands": { "app": { "lint": "yarn lint", "test": "yarn test" } },
  "grillWaivers": {
    "T2.webComponentGlobs": "shell-and-Markdown repo — there is no web-component surface to route",
    "T4.mutation-plumbing.app": "no unit-test surface yet; tracked in the backlog"
  }
}
