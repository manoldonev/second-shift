{
  "name": "review-toolkit",
  "description": "Parallel multi-agent code review: review-lead orchestration skill, reviewer-baseline shared protocol, generic reviewer agents (security, performance, maintainability, complexity, db, scope-completeness, test-coverage, plan, spec). Per-repo reviewer registry deltas and model-tier overrides come from .claude/second-shift.config.json; domain rules from documented extension files.",
  "version": "2.2.0",
  "author": {
    "name": "Manol Donev"
  }
}
