I read the changed files against the tree at `642a6b1`; script execution is sandboxed here, so everything below is derived by reading the production code, the register files, and the ablation generator's own arithmetic (I hand-recounted the 192-row firings table against every summary table — the generated block is internally consistent, so the numbers themselves check out).

## What holds up

- The `m4/head-missing` / `m4/head-tree-diff` deletion is genuinely *tighter*, not looser. `lean-evidence.sh:918` does refuse a keyless record at the merge boundary, and `cmd_verdict` (`lean-gate.sh:4307`) `envfail`s rather than omit the key — so the deleted arms were unreachable-with-effect. The fail-open on `CONTRIB_RC=*` that survives already applied to patch-id-bearing records before this PR, so no new hole.
- `(ac1)`'s new 18 / `111225555555555566` signature reconciles exactly against the 18 live `fail_milestone 4` sites.
- The observe seam is untouched by `lane_advisory`: `cmd_all`'s pre-pass calls only `cmd_1`/`cmd_4` (`lean-gate.sh:5029-5030`), so the unconditional `append_line` in `lane_advisory` can't write during an observe pass.
- The extraLanes `break` is correctly scoped to the inner `commands[]` loop, so `(i4)`'s "`echo three` never runs" still holds under the demotion.

## Warnings

**W1 — the re-cut corpus shares zero records with the 52 it replaces, and is largely older.** Old pin: `345`–`539`. New pin: `72`–`650`. The intersection is empty. 24 of the 70 scored records are from 2026-07-31/08-01, dozens of gate revisions *before* the surface this slice leaves — which is why `m1/spec-absent` splits 51 `attempt` / 32 `absent` (most records predate the absent verb). AC-9 says the re-cut exists "so that the next report measures the surface #642 left"; a corpus dominated by August-1 runs measures an older one. Two concrete casualties: `m4/patch-stale` and `m4/chain-break` now show **zero** firings, so the plan's "Explicitly out of scope" claim that they "carry the corpus's two sharpest dated incidents" is contradicted by the artifact in the same commit; and record `531`, the sole false-red datapoint retained finding **6** cites, is no longer pinned by any manifest row.

**W2 — `docs/testing.md`'s never-fired table is keyed to the old corpus.** Against the report this PR ships, four points are never-fired with no reachability row (`m3/lint`, `m4/chain-break`, `m4/patch-stale`, `m5/progress-current`), and two points the table lists as kept-because-never-fired (`m1/ledger-lint`, `m1/preflight-reconcile`) now record 4 and 2 firings. The table is honestly dated ("over a 52-record corpus"), so this is disclosure-adequate — but AC-2's register no longer covers the current never-fired set.

**W3 — an infra-dead selftest sweep now greens milestone 3 silently.** With `test` advisory, `run-selftests.sh` exiting the reserved `3` (every worker killed, nothing evaluated) records one advisory line and milestone 3 concludes `satisfied`. Previously that was rc=7 "re-invoke". Deliberate per D-3 and documented in `docs/testing.md`, but it is the one advisory case where the local answer isn't "correct-but-later" — it's "no answer at all", and the manifesto edit's own bar is that deleting the local refusal must leave the local answer correct.

**W4 — stale comments at `lean-gate.sh:4300` and `:4306`** still describe records degrading "to the pre-patch-id path" / "the pre-key records' path". That path is gone; such a record is now refused.

**W5 — AC-6 lands at −0.8% against a ≥30% bar.** D-6 discloses it and explicitly leaves the call to the operator rather than absorbing it, which is the right shape — but the AC is unmet and the PR cannot self-approve that.

## Nits

- The three new `gate-buckets.tsv` rows are appended at the bottom of the file, *under the `check-lean-chain.sh::note_violation` section header*, rather than in the `lean-gate.sh::block_milestone` / `::block_obligation` sections where their siblings sit. Lookup is by anchor so it works, but the file now reads as if two `lean-gate.sh` rows belong to `check-lean-chain.sh`.
- `lane_advisory`'s fixed suffix "the merge boundary re-runs this lane" is emitted for *every* extraLane. That's true for this repo's mutation lane; for an arbitrary consumer's extraLane it is an unverified claim printed as fact.

## BLOCKERS

1. **AC-3 is unmet: `cmd_close_out` still charges the fix budget for two of the six announcement reasons.** `plugins/dev-pipeline/skills/build-lean/lean-gate.sh:4852` — `resolve_open_pr || { fail_milestone 5 "$LEAN_PR_ERROR"; ... }` — and `:4848` (the `progress file is not current` copy). Both reason strings match the classes table's own patterns (`^(no (open|open or merged) PR found for branch|could not list PRs for)` → `m5/exit-artifacts:no-open-pr`; `^progress file is not current` → `m5/progress-current`), i.e. exactly the points AC-3 names and the report adjudicates `unchanged` 17/17 and 3/3. **Failure scenario:** a transient `gh pr list` error during three close-out attempts drives `append_attempt` three times on milestone 5; the fourth returns rc=4 and fires the hard-stop/abort path, on a refusal naming something no build fix can address — the precise defect AC-3 exists to remove. `scripts/gate-buckets.tsv` documents both surviving sites in its own rows, so this was seen and left. The reason it ships green is that the new `(ac1b)` guard (`lean-gate-selftest.sh:6130`) counts *block-verb sites only* — an inclusion-only assertion, structurally blind to an announcement reason still reachable through `fail_milestone`. Convert both sites (and add the matching exclusion-direction assertion, e.g. that no `fail_milestone 5 "` site carries one of the six reason prefixes).

2. **The reserved exit-3 cross-repo contract in `docs/config-schema.md` is now false and unamended.** Lines 22–33 still state that `3` "applies to the fixed `lint`/`typecheck`/`test` keys and to every `extraLanes` entry" and that milestone 3 "reds with exit `7` … charges **no fix attempt**, and the lean scheduler re-spawns the build session". After this change only `typecheck` reaches `lane_failure_class` (`lean-gate.sh:3855`); `lint`, `test` and every extraLane are advisory and classify nothing. **Failure scenario:** a consumer follows this doc, has its `test` lane exit 3 on a killed runner, and gets a green milestone 3 and a `satisfied` record instead of the documented "nothing was evaluated / re-invoke" — while the doc's stated exposure analysis ("bounded by the scheduler's `--max-continuations`") no longer describes the mechanism at all. This is the canonical contract doc, and `docs/testing.md`'s own reworded entry in this same PR still points readers to it "for the cross-repo contract". AC-9 requires falsified prose be updated.

3. **`plugins/dev-pipeline/skills/build-lean/SKILL.md:32` contradicts itself inside one sentence.** The bullet now reads "asserts milestone 5 — which a MERGED PR satisfies as well as an open one (#642), so close-out stays reachable after a merge — …" and then closes with "**leave the claimed label alone**: milestone 5 requires an open PR, so review is still in flight and the label is correct." **Failure scenario:** this is the checklist a build session executes; the surviving clause's premise is exactly what AC-8 deleted, so an agent reaching step 9 after a merge has one sentence asserting both that milestone 5 accepts the merge and that it cannot. The same falsified premise is load-bearing rationale at `docs/onboarding.md:26` ("The lean lane's exit milestone requires an open PR, so a session-side drop would fire while review is still in flight") — the stated justification for binding label release to the close event. AC-9 names `build-lean/SKILL.md` explicitly.

4. **`m5/identity-stamp` is converted with zero behavioral coverage, against AC-3's explicit "a fixture case per reason".** The two sites (`lean-gate.sh:4616`, `:4647`) route through `block_milestone`, but no case in `lean-gate-selftest.sh` or `scenario-liveness-selftest.sh` drives a `cmd_mark` failure at milestone 5 — the only mention is the comment at `lean-gate-selftest.sh:6127`. Every other converted reason got one ((c2b), (k3b), (k5b), (k9), plus (lean-absent-verdict)). **Failure scenario:** reverting either site to `fail_milestone` restores the fix-budget charge on an announcement-class refusal and nothing reds — `(ac1b)`'s count would drop to 7 only if *both* were reverted at once with no compensating block site, and no case asserts the verb or the zero-charge at all. Given blocker 1 is the same class of gap surviving the same guard, this one needs a real case, not a site count.
