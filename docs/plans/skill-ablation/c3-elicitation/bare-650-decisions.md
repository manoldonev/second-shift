I read the ticket against the tree at `4813e0b`: the committed criterion (`docs/plans/second-shift-643-preregistration.md`, revisions 2–4), the audit it scores (`docs/plans/second-shift-643-audit.md`), the scheduler (`plugins/dev-pipeline/skills/run-lean/orchestrate-lean.sh`), the gate's staleness arms (`plugins/dev-pipeline/skills/build-lean/lean-gate.sh:2485-2565`), and #590's landed template.

Here are the decisions I'd take back before planning.

**1. Which deliverable list governs.** The numbered scope keeps items 1, 4 and 5; the operator amendment departs 2 and 3 and then names five deliverables — per-launch spawn evidence, the staleness re-check, "variant (c) `--attended`", revision 5 of the criterion, and the committed evidence-file skeleton — three of which appear nowhere in the numbered scope, while the title still says "execute the arm #643's criterion selects". Undecided: is the amendment's list the AC set (and the numbered items now read through it), or the numbered scope plus three additions?

**2. What variant (c) mechanically is.** Scope 1 defines it as "keep only the spawns that genuinely need a model session, replace the rest with direct `lean-gate.sh` calls per #590's landed template" — but #590 already deleted the only such spawn. Today `orchestrate-lean.sh` spawns exactly two roles (`:762`, `:800`, `:929`), BUILD and REVIEW, both of which need a model session; there is nothing left to convert. The amendment's "variant (c) `--attended`" names a different thing entirely (a drive mode, not a spawn-elimination). What is (c)?

**3. What `--attended` means against the attendance contract.** Every spawn is stamped `LEAN_ATTEND_MODE=headless` (`orchestrate-lean.sh:632`), and `operator-override.sh:105-110` envfails on *any* positive value — attendance is minted by an operator in their own session and bound to session id plus run id, precisely so a payload cannot assert its own. So `--attended` cannot mean "spawn sessions marked attended". If it means the scheduler stops spawning and hands the payload to the operator's live session, then what it blocks on, how `RUN_ID` is established without a `claim` spawn, and how the two-identity separation `check-lean-chain.sh` enforces (build run ≠ verdict run) survives a single-terminal operator all need settling.

**4. Where the mid-run staleness re-check is called from.** `run-lean/SKILL.md:54` states as design that the check "has no channel into a live session, so it bounds a dead run at one wasted session rather than at zero" — and the ticket's "nothing calls it" is not accurate as written: `staleness` runs at preflight (`--arm ticket`, `:500`) and before every build spawn (`:780`). The missing call is mid-run, and the three sitings differ in kind: prose in `build-lean`'s checklist (unenforced — the exact class #535 shipped and the next run ignored), a fail-closed precondition inside the gate's milestone commands (which makes every milestone call require a `gh` read plus a fetch, so an unreadable tracker now reds a milestone that used to pass offline), or a scheduler-side supervisor that kills a live child mid-write. Only the gate-side siting survives arm A, which scope 5 says it must.

**5. What a mid-run fire is allowed to do, and which arms run.** The corrected class-M shape is a spawn alive across its ticket's close, so the ticket arm is the motivating one — but the base arm has its own mid-run case and its own cost (a fetch per call). And if the check aborts a turn mid-milestone, it leaves an uncollected worktree: the same shape `inflight` refuses on and the same shape the rubric charges as class T. Undecided whether firing aborts, records-and-continues, or stops at the next milestone boundary.

**6. Which per-launch remedy, and whether the deliverable is a filename or a launch record.** Scope 4 offers "its own log directory, or stamp `SPAWN_N` with a per-launch token" and picks neither. Beyond the name: the campaign needs launch start/end, exit code, rounds and models per launch, and today the scheduler's timestamped control lines (`now_iso`, `say`) go to the terminal only — nothing writes a control log. Is the deliverable a naming change, or a launch manifest the scheduler authors?

**7. What the per-launch token is, and what moves in lockstep.** `RUN_ID` is the obvious candidate but it is exported by the payload, cached per issue (`lean-gate.sh:918`), and deliberately *reused* on re-entry — so it may not be per-launch. Separately, `retro-corpus.sh:391` globs `"$dir/$tk"-lean-spawn-*.log` flat to emit `orchestrated` (#565 AC-12/13), and the 63-spawn corpus the audit was scored on is in that flat shape; undecided whether consumers and the hand-archive convention move with the layout or whether the layout must stay backward-readable.

**8. The evidence file's schema, its author, and whether a hand-typed number is admissible.** `.claude/pipeline-state/` is gitignored (`.gitignore:7`), so every committed figure is a transcription. Undecided: what a row is (launch? spawn? run?), what columns, who transcribes — and specifically for **operator-attention minutes**, which the criterion makes the *primary* metric, whether operator self-report is the record. Nothing in the lane can stamp attention mechanically, and this repo's standing posture is that only a committed, diffable artifact the author cannot forge counts as evidence.

**9. What revision 5 is for.** The amendment mandates one and doesn't say what it fixes. At least three candidates are live and each changes what this ticket builds: (i) the unit — the bands were computed per *spawn*, per `D-8`, while the criterion specifies *launches*, and item 4 is what finally makes launches enumerable; (ii) how the prospective runs combine with, or replace, the retrospective corpus — the ticket says resolving the post-#566 tension (nine spawns, `0.778`, arm B) "is this campaign's job", and the criterion has no rule for it; (iii) granularity at n=3, where `M1ᵗ` over three launches can only take `1.0 / 0.667 / 0.333 / 0`, so the `>= 0.80` keep row is reachable only by a flawless sweep and a single class-T lands arm B by arithmetic.

**10. The criterion never consumes variant (c)'s measurements.** Revision 4's table (`:265-271`) decides on `M1ᵗ` plus `attention(a)` vs `attention(b)`; variant (c) appears in no row, yet the campaign mandates three runs of it and arm B ("reshape") is presumably a reshape *onto* (c). Undecided what reading of (c)'s numbers would select B, and what happens if (c) measures worse than both. Related and worth fixing in the same breath: arms are A/B/C and variants are (a)/(b)/(c), and they do not correspond — arm C (keep) *is* variant (a).

**11. Whether the (c) spike lands on `main`, and on what removal condition.** Scope 1 says it "is an instrument, not a ship — it lands only if it wins", but #652 runs over days against installed plugins, so it has to be installed to be measured, and this repo's front door ships to a marketplace. Undecided: merge behind a flag or not, what the default is, whether the front-door docs move now (#643's `D-3` deferred AC-5 to "the follow-up, which is where a front door can actually move" — that follow-up is now split in two), and who deletes it if arm A or B wins, against a delete-first doctrine and a guard-budget trailer that would have to carry the added mass.

**12. The time box, and what #652 does if it expires.** "Time-boxed spike" with no box stated. If (c) isn't working when the box runs out, does #652 run the campaign with two arms and a documented gap, or does the campaign wait — and does an unbuilt (c) change what revision 5 must say?
