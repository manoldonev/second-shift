# Comparison 3 evidence

- `ticket-<n>.md` — the issue body handed to the bare arm. Note these are the **current** bodies,
  amended after the interviews they are scored against, so some reference answers were already in
  the text the bare arm read. That confound runs toward bare.
- `bare-<n>-decisions.md` — the bare session's decision list.
- `scoring.tsv` — per-row adjudication, with a `pre_flight_discoverable` column.

The kit arm's score is 100% by construction (it authored the reference) and is inadmissible by
registration. Bare recall 6/20. See `docs/skill-ablation.md` §3.
