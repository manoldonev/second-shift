# lockstep-manifest.tsv — contract pairs enforced by scripts/check-lockstep-pairs.sh.
#
# TAB-separated. Columns:
#   pair-id <TAB> relation <TAB> fileA <TAB> anchorA <TAB> fileB <TAB> anchorB
# relation: verbatim | subset-of   (see the script header for exact semantics)
# Paths are repo-root-relative. Blocks are delimited in-file by LOCKSTEP-BEGIN/END <anchor>.
#
# What belongs here: a contract duplicated across files where the ONLY thing keeping the
# copies together is a prose instruction. What does NOT: pairs already mechanically
# enforced elsewhere — model tiers (check-model-tiers.sh), the reviewer registry, the
# section catalog, the text-contract carriers, config-lint vs schema (the modelOverrides tier enum is driven
# from both sides in config-lint-selftest.sh — every schema-declared tier must be accepted,
# and config-lint's rejection message must name exactly the schema's enum). Duplicate
# machinery is worse than none.
#
# FINDINGS_SCHEMA triple. The Workflow runtime gives these scripts no import, so each
# declares its own copy; stall-probe and tool-discipline-probe exist to reproduce
# production behavior exactly, which is worthless if their schema has drifted. A triple is
# expressed as two pairs against the same canonical leg (code-review.mjs). (design-sync.mjs,
# which referenced the schema only in a comment, was never a leg; retired in #574.)
findings-schema-stall	verbatim	plugins/dev-pipeline/workflows/code-review.mjs	findings-schema	plugins/dev-pipeline/workflows/stall-probe.mjs	findings-schema
findings-schema-tdp	verbatim	plugins/dev-pipeline/workflows/code-review.mjs	findings-schema	plugins/dev-pipeline/workflows/tool-discipline-probe.mjs	findings-schema
#
# PROGRESSIVE_EMIT preamble pair (#283, narrowed from a triple in #348). Same
# Workflow-cannot-import class as the FINDINGS_SCHEMA triple above: code-review.mjs and
# intake-review.mjs each declare their own copy of the "emit as you go" nudge for their
# exhaustive-class reviewer(s), and a copy that drifts silently re-opens the #175/#183
# turn-cap-death class one file at a time.
#
progressive-emit-intake	verbatim	plugins/dev-pipeline/workflows/code-review.mjs	progressive-emit	plugins/dev-pipeline/workflows/intake-review.mjs	progressive-emit
#
# Pipeline-seam env denylist (#83, re-anchored in #348). preflight.sh and lean-gate.sh both
# spawn a consumer's configured lane commands as a child process and must scrub the same
# pipeline seam vars out of that child's env (a leaked SECOND_SHIFT_CONFIG / STATECTL_STATE_DIR
# / etc. re-roots or re-states a lane that is itself second-shift tooling — #34's ~20 spurious
# failures).
#
# preflight.sh is a superset of lean-gate.sh's list — it also scrubs its own
# PREFLIGHT_DOCTOR_CMD — so ONE subset-of row states the whole contract directly.
seam-scrub	subset-of	plugins/dev-pipeline/tools/preflight.sh	seam-scrub	plugins/dev-pipeline/skills/build-lean/lean-gate.sh	seam-scrub
#
# ---------------------------------------------------------------------------------------
# DROPPED to reviewer-guarded — recorded here rather than forgotten. Each was considered
# for the mechanical manifest and rejected for a stated reason.
#
# preflight ↔ gate zero-verifying-lane predicate. The coupling is real against
# lean-gate.sh milestone 3, which reds
# naming the opt-out where preflight only warns. preflight.sh computes an aggregated
# VERIFYING count inline; the gate reads allowUnverified / lanes / extraLanes into separate
# variables under a different jq arg name. No byte-identical block exists, and reaching one
# means restructuring working code for the benefit of its own guard. The intent is already
# declared in prose at preflight.sh's VERIFYING comment ("Keep this set in lockstep with
# lean-gate.sh").
#
# GH_BOT config-dir basename (install-gh-bot.sh ↔ claim-issue.sh): RETIRED by #92 —
# both call tools/gh-bot.sh; one ladder remains, so no pair to lockstep.
#
# args.config subset (the six Workflow dispatch sites' `config:` args + their explaining
# comments ↔ the `config.` reads inside the dispatched .mjs scripts). One contract — which
# config keys each script needs — split across a caller that states it in prose and a callee
# that consumes it in code. It has already failed in both directions: passing the whole parsed
# config killed a dispatch outright, and the practiced `{ reviewers: {} }` recovery serialized
# cleanly while silently disabling every model override, defeating a model-tier feature shipped
# long afterwards. Considered for a row and DROPPED: no byte-anchorable literal exists on either
# side — the caller side is six differently-worded prose comments inside six differently-shaped
# args objects across four files and two plugins, and the callee side is an expression
# (`(config && config.tracker && config.tracker.type) || 'github'`) whose shape varies per key.
# Forcing a shared literal would mean writing dispatch prose to satisfy a grep rather than the
# model that reads it. The BEHAVIOR is not reviewer-guarded: runtime-shim-selftest.mjs Case H
# executes the real code-review.mjs body under the documented subset alone and asserts both
# reads survive it — H1 that a modelOverrides value reaches the dispatched model, H2b/H3b that
# tracker.type still branches the scope-completeness fetch and that its absence flips the
# branch. Both halves are mutation-verified: hardcoding trackerType reds H2b, dropping the
# modelOverrides lookup reds H1. Only the prose WORDING is reviewer-guarded, which is the
# correct tier for it per the no-prose-presence rule.
#
# Test-tier map (CLAUDE.md "Where a new test goes" table ↔ docs/testing.md "Why a tier map at
# all" table). Two representations of one routing contract: a tier added to one and not the
# other sends the next author to the wrong file, which is how a tier accretes per-tool
# fixtures instead of a scenario. Considered for a row and DROPPED: the two are deliberately
# NOT parallel — CLAUDE.md's is a three-column router keyed by what you are guarding
# ("If you are guarding… | Write it as | Lives in"), docs/testing.md's is a three-column
# STATUS table keyed by the classic pyramid tier ("Classic tier | Here | Status"), and they
# carry different row sets on purpose (the status table lists Adversarial and Mutation, which
# are not places a new test goes). Forcing a shared anchorable literal would mean collapsing a
# router and a status board into one table that serves neither reader. Reviewer-guarded: both
# tables are short, sit in the two files every contributor reads first, and a new tier lands
# with its own suite in the same PR.
#
# LEAN_JOB_CEILING, writer ↔ reader (#526). lean-gate.sh EXPORTS that name into every
# milestone-3 lane child; tools/run-selftests.sh READS it. A one-sided rename breaks the
# coupling silently AND in the invisible direction — the runner sees no ceiling and every
# lane sizes itself to the whole machine again, which is exactly the pre-#526 behavior and
# so reds nothing that asserts correctness. Considered for a row and DROPPED: the two sites
# share a token, not an anchorable BLOCK, and pinning a variable name in two places is the
# presence check this repo bans. The behavioral guard is lean-gate-selftest.sh (jc1) — a
# real extraLane child echoes `${LEAN_JOB_CEILING:-unset}` and its value must equal the
# ceiling the gate announced, so a rename on either side leaves the child reporting `unset`.

# LEAN_SELFTEST_CACHE_DIR, writer ↔ reader (#563). The same coupling one ticket later, and
# DROPPED for the same reason: lean-gate.sh EXPORTS that name into every milestone-3 lane
# child and tools/run-selftests.sh READS it, but the two sites share a token, not an
# anchorable BLOCK, and pinning a variable name in two places is the presence check this repo
# bans. The invisible-direction risk is the sharper one here — a one-sided rename does not red
# anything, it just means no lean sweep ever serves from cache again, which looks exactly like
# a cache that is working and never hitting. Behaviorally guarded on BOTH sides:
# lean-gate-selftest.sh (sc1-sc3) spawns a real lane child that must report the store the gate
# announced, and run-selftests-selftest.sh's #563 cases drive the runner through the variable
# rather than the flag — so a rename on either side leaves a child reporting `unset` and the
# runner's env cases cold.

# Audit ledger row schema — the field list a consumer keys off (audit QUERIES.md schema
# section ↔ audit SETUP.md "What you get"). This pair is the reason the ledger drifted:
# for months QUERIES.md documented queries against `.args_excerpt.command` /
# `.args_excerpt.file_path`, a shape the hook never emitted, and told readers to "re-add
# tool_input capture" — a reader following the card got empty output and no signal that
# the card was wrong. Both sites now carry an identical one-line field list, which reads
# naturally in each (a schema note in the query card, a field list in the setup bullet),
# so `verbatim` holds without contorting either document.
#
# The THIRD copy — the hook jq object literal in audit-tool-calls.sh — is deliberately
# NOT in a row. A jq construction expression and a prose field list share no anchorable
# bytes, and forcing one would mean writing a line that serves neither the maintainer
# reading the emit nor the engineer reading the card. It is not reviewer-guarded either:
# audit-selftest.sh Test 9 asserts that a real emitted row's keys_unsorted equals the
# documented field list exactly, so a field added, renamed, or reordered in the hook
# without the docs following fails the suite. Mechanical on both legs, by two mechanisms.
audit-row-fields	verbatim	plugins/audit-toolkit/skills/audit/QUERIES.md	audit-row-fields	plugins/audit-toolkit/skills/audit/SETUP.md	audit-row-fields
#
# Audit ledger DIRECTORY resolution — the hook (writer) ↔ audit-history.sh (sweeper). Both
# must land on `--git-common-dir/..`, because the lean readers (lean-gate.sh's `entry`,
# lean-reconcile.sh) all do, and the writer disagreeing with
# them produced two opposite failures: an honest worktree run refused at `entry` for a
# ledger it had just written, and a verdict record naming a session reconcile could not
# resolve — which reads as forgery.
#
# TWO COPIES rather than one sourced helper on purpose: the hook fires on every tool call
# and stays dependency-free (no `source`, no PATH assumption, nothing to resolve at hook
# time), and audit-history.sh is a standalone slash-command script. Prose was the only
# thing that would otherwise hold them together, which is exactly this file's remit. The
# THIRD site is deliberately NOT a row: lean-gate.sh derives `MAIN_ROOT` for many
# purposes beyond the ledger, so it shares no anchorable bytes with this block. It is
# held by fixtures instead — lean-gate-selftest.sh and lean-reconcile-selftest.sh
# each drive the REAL hook from a linked worktree and assert their reader finds the result,
# so a writer-side drift reds a reader's suite.
audit-ledger-dir	verbatim	plugins/audit-toolkit/hooks/audit-tool-calls.sh	audit-ledger-dir	plugins/audit-toolkit/scripts/audit-history.sh	audit-ledger-dir
#
# Decomposition-economy rule (the run-cost bias). One canonical author — the
# intake-orchestrator, which owns the verdict — mirrored into decomposition-reviewer,
# which reviews splits against the same standard and must not drift into a softer one.
# Two live copies rather than a cross-reference: each skill is loaded on its own, into
# a session that has not read the other, so a pointer would resolve to nothing at the
# moment the rule is needed. That is exactly the "only a prose instruction keeps them
# together" case this manifest exists for.
decomposition-economy	verbatim	plugins/intake-toolkit/skills/intake-orchestrator/SKILL.md	decomposition-economy	plugins/intake-toolkit/skills/decomposition-reviewer/SKILL.md	decomposition-economy
#
# figma node-resolution discipline (figma-faithful/SKILL.md steps 1-2 + "Figma capability"
# section <-> figma-faithful-spec/SKILL.md). The spec skill used to restate both, under an
# explicit prose sync liability ("Keep the two in sync if either changes") -- the exact
# only-a-prose-instruction case this manifest exists for. Considered for a row and DROPPED:
# the coupling was removed rather than guarded. figma-faithful is now the canonical home and
# the spec skill carries the operative one-liner plus a by-name pointer (the shape
# figma-iterate/SKILL.md already used), so there is no second copy left to byte-anchor. The
# deltas that remain in the spec skill are genuine divergences, not copies: it also uses
# get_code_connect_map, its terminal sentence forbids transcribing from a static image, and
# it deliberately omits figma-faithful's parent-frame capture (an implementation concern).
# A verbatim row here would fail on the first legitimate edit to either side.
#
# Artifact-reviewer baseline deltas (figma-faithful-spec-reviewer <-> figma-faithful-plan-reviewer).
# Both adopt review-toolkit:reviewer-baseline but must override the same two things the same way:
# the merge-gating Critical/Warning/Pre-existing ladder does not apply to an artifact graded
# before implementation, and their dispatchers ran them schema-free (figma.mjs, until #574 retired it) so the baseline's Output Mode
# StructuredOutput instruction is wrong for them. Two live copies rather than a cross-reference:
# each agent is loaded alone, into a context that has not read the other, so a pointer would
# resolve to nothing at the moment the override is needed -- the same reasoning as
# decomposition-economy above. A one-sided edit re-opens the drift #258 closed.
artifact-reviewer-baseline-deltas	verbatim	plugins/design-toolkit/agents/figma-faithful-spec-reviewer.md	artifact-reviewer-baseline-deltas	plugins/design-toolkit/agents/figma-faithful-plan-reviewer.md	artifact-reviewer-baseline-deltas
#
# check-pipeline-chain.sh REQUIRED_MARKERS is now the SOLE carrier of the marker list —
# the generated `case` region and the schema table that held the other copies are both
# gone, so there is no pair left to express. check-pipeline-chain-selftest.sh asserts the
# list parses non-empty, so a rename fails loudly instead of passing on an empty set. The
# LOCKSTEP-BEGIN/END markers are left in place so a future row is cheap if a second
# carrier ever appears.
#
# schema planFilePattern default <-> preflight.sh's hardcoded copy of that literal. A real
# coupling: the copy is the fallback used when a consumer sets no override, so a one-sided
# edit resolves a path the schema no longer publishes. Considered for a row and DROPPED: the
# canonical side is schema/second-shift.config.schema.json, and JSON has no comment syntax, so
# a LOCKSTEP-BEGIN/END marker cannot be placed there at all -- the pair is unanchorable by
# construction, not merely awkward. Guarded behaviorally instead: preflight.sh resolves the
# pattern through the same substitution the stages use and PRINTS the result, and preflight-
# selftest.sh run 18 ASSERTS on that printed line -- both that an unmigrated override has its
# retired token stripped and that a migrated pattern is left untouched (the over-match
# negative). Printing alone would not have been coverage; the assertions are. Revisit only if
# the schema default is ever mirrored into a comment-bearing file.

# lean branch prefix — RETIRED (#413), recorded here rather than silently dropped, because the
# reasoning that justified dropping it is the reasoning that has now been made load-bearing.
# The pair was ci.yml's LEAN_BRANCH_PREFIX env constant ↔ lean-gate.sh's runtime derivation of a
# `lean/` namespace from tracker.branchPrefix. It was DROPPED as non-byte-anchorable (a YAML
# scalar against a shell transform that never spells the value), and what made dropping it SAFE
# was that check-lean-chain.sh did not classify on the prefix alone — it also applied on
# lean-artifact presence, so a stale constant could not silently exempt a lean PR.
# That compensating control is now the WHOLE rule: the lean lane cuts `<branchPrefix><key>`
# branches like the staged lane, there is no second prefix, and applicability is the key-matched
# lean spec in the PR's diff and nothing else. So there is no longer a pair here to pin OR to
# drop — the constant and the derivation both ceased to exist, along with the mutual
# non-prefix-match property both sides used to assert.
# Nothing replaces this entry at the chain gates. The one classifier (lean-evidence.sh) is
# DELEGATED to by both of them rather than copied into them, so the successor coupling there is
# a function call, which no manifest row can improve on. One site outside those gates re-states
# the test instead of calling it, and it gets its own note directly below.

# lean artifact discriminator: lean-evidence.sh classify() <-> retro-corpus.sh open-prs (#413).
# A REAL coupling and the only re-statement of the test that survives this change. Both sides
# decide "is this PR lean" the same way -- exclude fixture paths, then require a basename ending
# `-<key>-lean.md` -- and a one-sided edit (a fourth fixture directory, a renamed suffix) leaves
# the retro corpus silently reporting live lean PRs as verdict-less, or sweeping staged ones in.
# It is NOT delegable, which is why the copy exists: the gates classify the PR they are running
# ON, from a PR context (head ref, base ref, body) that lets classify() resolve one key and diff
# one range. `open-prs` classifies a LIST of other PRs from a single `gh pr list --json files`
# call, where no such context exists and an open PR's spec is committed on its own branch rather
# than the base, so a working-tree file test would reject every candidate it exists to find.
# D-12 makes it a discriminating site; D-13 scopes delegation to check-pipeline-chain.sh.
# Considered for a row and DROPPED: the two sides share no literal to anchor. One spells the
# test as shell `case` patterns over a `find` walk, the other as a `grep -v` chain plus a `grep
# -qE` over a JSON array, and the suffix is a variable on one side and inlined in a pattern on
# the other. A LOCKSTEP-BEGIN/END pair would put a `verbatim` relation between two dialects,
# which is wrong by construction, and inventing a shared constant purely to be compared is the
# duplicate-machinery case this file's header rejects.
# Guarded behaviorally instead, on both sides and against the same two mistakes: lean-evidence-
# selftest.sh (d)/(z2) pin the key match at the classifier, and retro-corpus-selftest.sh (AC-5b)
# drives `open-prs` over a fixture PR array carrying another ticket's spec and a fixture-pathed
# spec and asserts neither casts a vote -- with (AC-5) as the non-vacuity side, where a real lean
# PR IS selected. Revisit if either side ever grows a shared literal -- most plausibly if the
# `-lean.md` suffix is hoisted into the config schema.

# per-ticket corpus dedup (#289) — RETIRED. retro-corpus.sh is the sole carrier of the
# basename-equals-ticketKey supersedes rule, so there is nothing to hold it in lockstep with.
# Its own behavior stays guarded by retro-corpus-selftest.sh (289 AC-1)/(AC-2)/(AC-3), which
# pin live-supersedes-snapshot, orphan-snapshots-are-distinct and dedup-before-window.

# lean verdict-record key schema (`verdict=`, `run_id:`, `session_id:`, `reviewed_head:`,
# `reviewed_patch_id:`, `inherited_patch_id:`, `inherited_from_verdict:`, `fidelity:`) — ONE
# writer (lean-gate.sh's `verdict` subcommand) and THREE readers: lean-gate.sh milestone 4,
# scripts/check-lean-chain.sh, and lean-reconcile.sh. A real coupling: dropping a key on the
# writer silently un-satisfies all three readers, and a reader-side requirement the writer
# never emits reds every lean PR. Considered for a row and DROPPED — the writer spells the
# keys as `echo` lines and the readers as grep/jq patterns, so there is no literal both sides
# could share without inventing a canonical constant purely to be compared, which is the
# "duplicate machinery is worse than none" case this file's header names.
# Guarded behaviorally instead, and the guard COMPOSES across the sites rather than pinning
# bytes: lean-gate-selftest.sh (p5)/(p7) drive the writer and then feed its output to the
# milestone-4 reader in the same run, so a writer that drops a key fails at the reader;
# (j3)/(j3b)/(u1) pin each key's absence as its own refusal, and check-lean-chain-selftest.sh
# (N2)/(N3)/(R1) and lean-reconcile-selftest.sh (J3)/(K1) do the same at the other two readers.
#
# `reviewed_head:` and `reviewed_patch_id:` are the two derived keys — they carry a value the
# readers RECOMPUTE rather than merely extract, so their writer/reader coupling is tighter than
# the other three's: a writer that stamped, say, a short sha would still extract cleanly
# everywhere and then fail every comparison. `reviewed_patch_id:` is the tighter of the two,
# because both sides must agree on the base, the diff range AND the excluded path; a divergence
# in any of the three yields two well-formed values that never match. Neither side can pin that
# with a shared literal, so it is composed instead: lean-gate-selftest.sh (u3) and the (v) block
# drive writer-to-reader end to end, and scenario-liveness-selftest.sh's (lean-declared) and
# (lean-patch-id) legs compose each arm against a record whose INFERRED freshness is green —
# the only shape where the declared arm is the sole thing that can red.
#
# `verdict=` is read FIRST-MATCH at every reader, never counted across the file: the writer
# appends the reviewer's prose below the keys and review prose quotes verdict values, so a
# count-anywhere reader passes a record whose authoritative first line says needs-work.
# lean-gate-selftest.sh (s) and check-lean-chain-selftest.sh (P) drive exactly that record
# through both readers.
#
# Revisit if a fourth reader lands, or if any site starts parsing the record as structured
# data. (An earlier revision of this note credited lean-reconcile-selftest.sh (C)/(J1)/(J2)
# with the missing-key guard at that reader. They do not carry it — (C) removes the LEDGER for
# a session the record names, and (J1)/(J2) collide two supplied identities — so (J3) was
# added to make the claim true rather than the claim softened to match the coverage.)
#
# `inherited_patch_id:` and `fidelity:` are the schema's OPTIONAL-VALUED keys and the only ones
# not read first-match — they get the two rows below rather than a line in this note, because
# unlike the rest of the schema the extraction IS byte-anchorable. See that entry.
#
# `fidelity:` (#394) is behaviorally guarded the same composed way as the rest: lean-gate-selftest.sh
# (fd1)-(fd7) drive the writer's enum, its unconditional emission and the fail-x-approve refusal,
# then feed the result to the milestone-4 reader; check-lean-chain-selftest.sh (X3)/(X4) do it at
# the merge boundary. Its VALUE is armed-ness-relative, which no literal can pin: `pass` is
# required on an armed spec, refused-unless-`not-applicable` on an unarmed one, so the writer and
# both readers must agree about arming as well as about the key — see the `lean-design-armed` row.

# lean inheritance-key extraction (#375) — the `inherited_key` awk program, held identically by
# the writer's own reader (lean-gate.sh milestone 4 and its chain walk) and by the two other
# readers of the same record. TWO rows for THREE copies: `verbatim` is a pair relation, so
# lean-gate.sh is the canonical side of both.
#
# A row rather than a DROPPED note, which is what separates this from the schema entry above:
# there the writer spells keys as `echo` lines and the readers as grep patterns, so no literal
# could be shared without inventing one. Here the three copies are already the SAME literal —
# an awk program, in a file-format-neutral form that needs no adaptation to the host script's
# dialect (lean-gate.sh and lean-reconcile.sh are `[ ]`-portable, check-lean-chain.sh is bash).
# There is nothing to invent, so declining the row would only be declining to check it.
#
# The coupling is real and one-directional in the dangerous way: a reader that keeps the OLD
# first-match extraction still parses every correct record identically and diverges only on the
# adversarial one, so drift here is invisible to every green run. That is precisely the
# "silently stops being checked" shape this file exists for — and it is not hypothetical, since
# the three-reader agreement is what made the pre-fix blind spot uniform rather than caught.
#
# Behavioral guards sit under it at each reader and compose writer-to-reader: lean-gate-selftest.sh
# (z1)/(z2)/(z3), check-lean-chain-selftest.sh (V6)/(V6b), lean-reconcile-selftest.sh (N7)/(N7b).
# The rows catch what those cannot — a fourth reader added later with a hand-copied extraction
# that drifts, or a copy edited here without its counterparts.
#
# NOT covered by these rows, and deliberately: the chain-WALK loop around this extraction, which
# each reader also holds a copy of. Considered for a row and DROPPED — the three are not one
# literal and cannot be made into one without harm. Each phrases its own diagnostic (the merge
# boundary names a remedy, `lean-reconcile.sh` adds the independence arm no other reader makes),
# each uses its host's list idiom, and check-lean-chain.sh must additionally scope `git log` to
# `$PR_HEAD_SHA` because CI's checkout carries base-side history the PR never authored. Forcing
# them verbatim would delete those differences, which are the point. Guarded behaviorally from
# three sides instead: lean-gate-selftest.sh (x6)/(x7)/(y2), check-lean-chain-selftest.sh
# (V3)/(V3b)/(V4)/(V5), lean-reconcile-selftest.sh (N3)/(N6).
# Revisit if a fourth reader lands.
lean-inherited-key-chain	verbatim	plugins/dev-pipeline/skills/build-lean/lean-gate.sh	lean-inherited-key	scripts/check-lean-chain.sh	lean-inherited-key
lean-inherited-key-reconcile	verbatim	plugins/dev-pipeline/skills/build-lean/lean-gate.sh	lean-inherited-key	plugins/dev-pipeline/skills/build-lean/lean-reconcile.sh	lean-inherited-key

# lean design ARMING (#394) — `design_armed`, the awk predicate that decides whether a ticket's
# committed spec arms the design render lane. Held by lean-gate.sh (which gates on it at
# milestones 1, 3 and 4) and by scripts/check-lean-chain.sh (which gates the merge on it).
#
# The coupling is unusually sharp because the two readers have DIFFERENT INPUTS available and
# must still agree. The gate ANDs this against config `design.provider`; the boundary cannot —
# the config is gitignored on every consumer and never reaches a CI checkout — so the boundary
# runs this predicate alone. Divergence is therefore invisible from either side: a boundary that
# read arming more narrowly would wave an armed PR through with no render evidence, and one that
# read it more widely would red honest unarmed work, and in both cases the OTHER site stays green
# and says so.
#
# Only the NARROW decision is shared, deliberately. The gate's richer form validation — a
# provider handoff link, the neither-armed-nor-disarmed refusal, the reason required on a disarm
# — is authoring feedback it gives at milestone 1, so a spec failing any of it never reaches a
# merge and the boundary needs no opinion about it. Sharing those too would force the boundary to
# carry refusals it can neither act on nor explain.
#
# Behavioral guards on both sides: lean-gate-selftest.sh (dz1)-(dz7) at the gate,
# check-lean-chain-selftest.sh (X1)-(X6) at the boundary, and scenario-liveness-selftest.sh's
# three (lean-design-*) legs compose the writer's manifest through both readers.
# Revisit if lean-reconcile.sh grows the receipt arm (#394 defers it) — it becomes a third copy.
lean-design-armed	verbatim	plugins/dev-pipeline/skills/build-lean/lean-gate.sh	lean-design-armed	scripts/check-lean-chain.sh	lean-design-armed

# intake-receipt vocabulary (Kind enum, open-region disposition enum, surface disposition
# enum, the two explicit empty forms, intent-gap record schema) —
# `interviewing-baseline/SKILL.md` states it in prose and tables; `ledger-lint.sh` holds the
# only machine copies (KIND_ENUM, DISPOSITION_ENUM, SURFACE_DISPOSITION_ENUM, OPEN_EMPTY_FORM,
# SURFACE_EMPTY_FORM), and `scripts/check-lean-chain.sh` reads
# the record's `ratified:` / `ratified_by:` keys. A real coupling: a Kind value added to the
# doc and not the lint is a value the receipt gate rejects with a message naming the enum the
# author just read. The empty forms are the sharper case — the lint compares them with
# `grep -qF`, so a doc-side reword of one word makes the form the author copied unusable.
# Considered for a row and DROPPED: the doc side is a markdown table of prose descriptions,
# not a quoted literal — `verbatim` would compare a table against a shell assignment, and
# `subset-of` reads the FIRST quoted literal in a block, of which the doc has none. Inventing
# a canonical constant on the doc side purely to be compared is the "duplicate machinery is
# worse than none" case this file's header names. The empty forms ARE quoted literals on the
# lint side but sit inside fenced code blocks on the doc side, where neither relation reaches.
# Guarded behaviorally instead: ledger-lint-selftest.sh drives all three Kind values, both
# open-region dispositions and both surface dispositions against real receipts (ll-o through
# ll-as), including each empty form and each refusal, so an enum or form edit that breaks the
# contract fails there rather than drifting; check-lean-chain-selftest.sh (R0)-(R4) does the
# same for the record's two keys at the merge boundary.
# NOTE the deliberate NON-coupling: the lean chain gate checks ratification ONLY and does not
# re-validate `disposition:` against either enum. That is what keeps them single-sited — a
# second copy in CI would create exactly the pair this entry declines to create.
# THE SKILL LAYER IS A CALLER CLASS OF ITS OWN: `intake-orchestrator/SKILL.md` Step 5.5
# prescribes the receipt shape and then runs `ledger-lint.sh --receipt` on exactly what it just
# prescribed, and `intake-interviewer/SKILL.md` prescribes the same shape for its ledger seed.
# Neither is an automated caller, so no CI lane reds when the lint tightens past what they
# describe — the exit gate simply becomes unpassable at agent runtime, where nobody is watching
# a build. A change that adds or tightens a mandated section MUST move both, and the check is
# empirical: build a receipt verbatim to the prose and lint it.
# Revisit if a third site starts parsing any of these enums.

# ticketTag semantics — three sites state it: `docs/config-schema.md`'s topology
# row/principles, the schema's own `ticketTag` `description` (renders live in every consumer's
# editor via the config's `$schema` key), and `run-lean/SKILL.md`, the lane that actually reads
# it. A real coupling: a doc claiming a reading the lane doesn't have (or vice versa) ships the
# exact wrong-but-plausible surface the re-scope exists to kill.
#
# The lane's reading is advisory only — it routes an intake decision, it does not gate a run,
# and the docs must describe it that way.
# Considered for a row and DROPPED: neither relation can express this pair. The doc side is
# markdown prose across two files, the schema side is a JSON string, and the lane side is prose
# in a SKILL — no single quoted literal is common to all three, so `verbatim` would compare
# unlike shapes and `subset-of` has nothing to anchor the FIRST quoted literal against.
# Inventing a canonical constant purely to be compared is the "duplicate machinery is worse than
# none" case this file's header names.
# Guarded behaviorally instead: `check-config-shadowing.sh` pins `run-lean/SKILL.md` to
# `ticketTag`, so an edit that stops reading the field there fails that check.
# Revisit if a fourth site starts parsing or restating `ticketTag`'s semantics.

# cross-plugin sibling-resolution ladder (#419) — three rungs (monorepo path → same-version
# sibling in the cache → newest sibling version carrying the file).
# `resolve_sibling()` in plugins/dev-pipeline/tools/resolve-sibling.sh is the
# canonical side (extracted from pipeline-doctor.sh in #562 so lean-gate.sh's
# resolve_ledger_lint() reuses it too — a caller-computes-its-own-hop-count split, not a
# second ladder; see that file's header). The hand-maintained .mjs mirror this entry used
# to hold beside it — `resolveSibling()` in design-sync-selftest.mjs, the
# only-a-prose-instruction case this manifest exists for — was retired with that suite in
# #574, which also retires the two-language drift risk and the mirror's lexicographic-vs-
# numeric version-ordering divergence this entry tracked. The RUNG ORDER remains the
# contract: the last rung is what hits in a real install, where plugins carry independent
# versions. Still DROPPED as a row (one implementation left; nothing to compare).
# The behavioral guard the bash side has, since #562 r2, is a unit-level equivalent covering
# the part the mirror's own rung-2 comment called easy to miss: pipeline-doctor-selftest.sh's
# (rs1)/(rs3) drive the ladder against a fabricated cache at BOTH bash consumers' real depths
# (pipeline-doctor.sh at tools/, lean-gate.sh's resolve_ledger_lint() at skills/build-lean/),
# lifting each caller's hop arithmetic by sentinel rather than injecting its results — because a
# fixture that supplies PLUGIN_DIR/PLUGINS_DIR covers the ladder and no caller, which is how a
# structurally dead rung 2 shipped on one consumer while reading as shared.
# The two candidates this entry flagged — preflight-selftest.sh:38 and doctor-selftest.sh:102 —
# have since been fixed the sanctioned way, and a fourth and fifth copy arrived with them. Each
# was considered for a row and DROPPED, for a reason the copy itself supplies:
#   preflight-selftest.sh, doctor-selftest.sh (review-toolkit)   `resolve_sibling_plugin_root`
#     RE-DERIVED, not copied, against CHECK-MODEL-TIERS.SH: that copy lives one level under its
#     plugin root and uses two and three hops, these two live three levels under theirs and use
#     four and five. The hop constants ARE the contract and they legitimately differ, so
#     `verbatim` against check-model-tiers.sh would compare unlike blocks and fail on a correct
#     tree. Against EACH OTHER they do not differ — same depth, same four/five rungs, same body —
#     which is precisely the convergence the revisit trigger at the end of this entry names. So
#     that pair IS pinned, by the row below. The one thing that had differed was which variable
#     each read its anchor from (`HERE` vs `SCRIPT_DIR`); the anchor is now a parameter, which
#     is what makes the blocks byte-anchorable at all.
#   doctor-selftest.sh (claims-lint.sh)                          `resolve_sibling_file`
#     Same divergence, keyed on a file rather than a marker dir.
#   check-emit-deadline.sh                                       the ENUMERATION variant
#     Shares a directory with check-model-tiers.sh, so the hop constants do transfer — but it
#     walks an unbounded set of plugin names instead of resolving one, and unions both layout
#     shapes rather than taking the first that hits. There is no identical block to pin without
#     carrying a dead copy of `resolve_sibling_plugin_root` purely to be compared, which is the
#     "duplicate machinery is worse than none" case this file's header names.
# Guarded behaviorally instead, and better than the .mjs side is: all three sites now run under
# tools/install-topology-selftest.sh from a staged cache — that is what drained their
# install-topology-known-red.tsv rows — and check-emit-deadline-selftest.sh's B6-B9 drive the
# real script from staged monorepo and cache shapes without leaving the ordinary sweep.
# The bash residual named above narrows but does not close: resolve-sibling.sh's ladder is
# still driven from no install cache by any suite (pipeline-doctor-selftest.sh's (rs1)/(rs3)
# cases do drive it against a fabricated cache at both consumers' depths — a unit-level guard,
# not the install-topology integration this paragraph is about).
# Revisit if a SIXTH site grows the ladder, or if any FURTHER pair converges on identical hop
# constants — such a pair is byte-anchorable and should be pinned, as the row below pins the
# first one that did.
cross-plugin-sibling-plugin-root	verbatim	plugins/second-shift/skills/doctor/tools/doctor-selftest.sh	cross-plugin-sibling-plugin-root	plugins/dev-pipeline/tools/preflight-selftest.sh	cross-plugin-sibling-plugin-root

# The unclaim workflow pair. The marketplace repo runs the SHIPPED unclaim script in place,
# so there is one script and no script pair — but there are two workflow FILES, and this is
# the first template workflow with a near-twin under .github/workflows/. They are allowed to
# differ in `name:`, the checkout pin (v5 here, v4 in the template, matching the precedent
# each side already sets) and the script path. They are NOT allowed to differ in the three
# blocks below, each anchored separately because the prose documenting it sits between them
# and is deliberately file-specific:
#   trigger      — a file that fires on a different event is a different workflow, and the
#                  consumer would inherit behavior the canary never exercised.
#   permissions  — the entire blast radius. A widening on one side that the other does not
#                  carry is the drift with real consequences; nothing else here has any.
#   env          — the token source and the injection discipline. `${{ }}` reaching a `run:`
#                  body on one side only is exactly the divergence a reader would not see.
# `verbatim` collapses whitespace, so the differing indentation of the env block against its
# two top-level siblings is not part of the contract; the tokens are.
unclaim-workflow-trigger	verbatim	.github/workflows/unclaim-on-close.yml	unclaim-workflow-trigger	plugins/second-shift/templates/consumer/second-shift-unclaim.yml	unclaim-workflow-trigger
unclaim-workflow-permissions	verbatim	.github/workflows/unclaim-on-close.yml	unclaim-workflow-permissions	plugins/second-shift/templates/consumer/second-shift-unclaim.yml	unclaim-workflow-permissions
unclaim-workflow-env	verbatim	.github/workflows/unclaim-on-close.yml	unclaim-workflow-env	plugins/second-shift/templates/consumer/second-shift-unclaim.yml	unclaim-workflow-env

# lean branch-prefix DERIVATION (#359) — DELETED WITH ITS SUBJECT (#413). The row pinned
# `lean_branch_prefix()`, the function that re-rooted `tracker.branchPrefix` onto a `lean/`
# namespace, across lean-gate.sh and lean-evidence.sh. Both copies are gone: the lane cuts
# `<branchPrefix><key>` like the staged lane, so there is no second namespace to derive and no
# mutual-non-prefix property to hold. The coupling the row protected — harness and boundary
# agreeing on one namespace — is now vacuous, because there is only the one both already read
# from `tracker.branchPrefix`. See the retirement note above for the ci.yml half of the story.

# lean PR-MARKER token (#359) — `LEAN_PR_MARKER_TAG`, the stage token identifying the comment
# that carries a build run's identity on the pull request. WRITER: lean-gate.sh's `mark`
# subcommand. READER: lean-evidence.sh's identity arm. Neither file can see the other's
# spelling, and CI resolves them from different places (an installed plugin vs a fetched blob at
# a pinned ref), so nothing but this row keeps them equal.
# The failure is asymmetric and both halves are bad: a renamed WRITER empties the reader's marker
# set, which the reader treats as "no marker" and refuses — every honest PR reds; a renamed
# READER would do the same. A reader that instead failed open would accept a verdict written by
# the build session itself, which is the property the marker exists to disprove.
# Behavioral guards on both sides: lean-gate-selftest.sh (pm1)-(pm7) at the writer,
# lean-evidence-selftest.sh (m)-(q) at the reader, and check-lean-chain-selftest.sh (Y1)-(Y3)
# composes a fixture trail through the delegating boundary.
lean-pr-marker	verbatim	plugins/dev-pipeline/skills/build-lean/lean-gate.sh	lean-pr-marker	plugins/dev-pipeline/skills/build-lean/lean-evidence.sh	lean-pr-marker

# lean PRODUCER CAPABILITIES (#445) — the claim comment's stage tag, the generation stamp's key,
# and the closed capability vocabulary an arm may bind itself to. lean-gate.sh is the CANONICAL
# side: it is the sole writer of all three, and the two rows below are its two readers, following
# the `lean-inherited-key-*` precedent for one writer with two readers.
# WHY IT MUST NOT DRIFT. An arm and its producer travel by DIFFERENT transports — the arm by git
# ref at a consumer's pinned marketplace ref, the producer by versioned plugin install into an
# operator's cache — and both report the same version, so nothing else can observe the skew. A
# renamed `LEAN_CLAIM_MARKER_TAG` empties check-lean-chain.sh's claim set (every honest PR reds)
# AND makes lean-evidence.sh find no stamp, which silently sends every capability-bound arm inert
# — a weakened boundary that reads exactly as green as a strong one. A renamed key, or a
# vocabulary that gained a token on one side alone, does the same quietly.
# Each side validates its OWN token against the vocabulary — the writer the subset it ships, the
# reader the token its arm requires — so a one-sided edit reds loudly on the side that made it.
# TWO files hold an unbound copy of the TAG and are deliberately not rows, on one reasoning:
# neither is a merge-boundary gate, and drift in either fails CLOSED and loudly instead of
# silently weakening a boundary — which is what earns a row in the first place.
#   lean-reconcile.sh              an operator-run reconciler; it carried that copy already.
#   run-lean/orchestrate-lean.sh   the scheduler's #500 re-entry probe. A drifted tag there stops
#                                  re-entry from being recognized, so the operator meets a
#                                  preflight reject on the next stopped run — never a green PR.
# Behavioral guards: lean-gate-selftest.sh (pc1)-(pc3) at the writer, lean-evidence-selftest.sh
# (ad1)-(ad7) at the bound arm, and check-lean-chain-selftest.sh composes a stamped trail through
# the delegating boundary.
lean-producer-capabilities-evidence	verbatim	plugins/dev-pipeline/skills/build-lean/lean-gate.sh	lean-producer-capabilities	plugins/dev-pipeline/skills/build-lean/lean-evidence.sh	lean-producer-capabilities
lean-producer-capabilities-chain	verbatim	plugins/dev-pipeline/skills/build-lean/lean-gate.sh	lean-producer-capabilities	scripts/check-lean-chain.sh	lean-producer-capabilities

# lean GATE-OUTPUT dispositions (#443) — `LEAN_OUTPUT_DISPOSITIONS`, the closed vocabulary a
# class-(b) line ("this arm could not evaluate") may name. Both merge-boundary gates declare their
# own copy: scripts/check-lean-chain.sh cannot source the payload it delegates to (it fetches it
# at a pinned ref in a consumer, and resolves it from the repo root here), and lean-evidence.sh
# must stand alone in a consumer's CI where this file does not exist at all.
# WHY IT MUST NOT DRIFT, and why a reader is the injured party rather than a writer: the two
# tickets after this one emit into class (b) from BOTH files, and the disposition is the token
# their readers branch on. A vocabulary that gained a word on one side alone would have that word
# reach a reader with no rule for it — which is a silently weakened boundary, the exact class this
# gate exists to make loud. Neither file can see the other's spelling.
# `verbatim` compares the ENTIRE marker block, which is why both sides declare the literal under
# the same variable name with no comments between the markers — the emitter that consumes it, and
# the prose explaining it, sit deliberately OUTSIDE the block on each side.
# Behavioral guards on both sides: lean-evidence-selftest.sh (dd1)/(dd2) drive the real emitter
# bytes over an accepted and a rejected disposition, and both suites anchor every class-(b) line
# whole against the same four-value alternation.
lean-output-dispositions	verbatim	scripts/check-lean-chain.sh	lean-output-dispositions	plugins/dev-pipeline/skills/build-lean/lean-evidence.sh	lean-output-dispositions

# lean ARM CUTOFFS (#444) — the two `since:` comparators. An arm declares the instant its
# contract took effect, so a run whose observation point precedes it is exempted rather than
# refused for missing a contract that did not yet exist. lean-evidence.sh compares
# `PR_CREATED_AT` against `LEAN_IDENTITY_SINCE`; lean-gate.sh compares a branch's first-commit
# author date against `ENTRY_SINCE`.
# The duplication is FORCED by the deployment shape, not chosen. A consumer's CI checkout has
# lean-evidence.sh and nothing else — second-shift-ci-check.sh fetches that one file at the
# pinned marketplace ref and never fetches lean-gate.sh — so neither file can source the other,
# and the gate's copy has no consumer-side existence at all.
# Considered for a row and DROPPED, and not for want of an anchor. The two comparators are not
# one contract: the payload compares an already-UTC `PR_CREATED_AT` supplied by the workflow,
# while the gate normalizes a git author date carrying an arbitrary offset and must pass it
# through `TZ=UTC git log --date=format-local` to become comparable. A `verbatim` row would bind
# them to a shared shape neither side needs and would fail on a correct tree; `subset-of` reads
# the FIRST quoted literal of each block, which here is a lone ISO instant rather than an enum,
# so it would compare two deliberately DIFFERENT dates and red. The `since:` values are MEANT to
# differ — each anchors to the merge that made its own arm binding.
# Follows the `lean evidence TOKEN SCOPES` precedent below: coincidence of present need is not a
# contract, and both sides have live signal. lean-evidence-selftest.sh (ac1)-(ac6) drives the
# payload's comparator across its cutoff, lean-gate-selftest.sh (eb1)-(eb7) drives the gate's
# including two non-UTC offsets in both failure directions, check-lean-chain-selftest.sh
# (Z1)/(Z2) composes the payload's through the delegating boundary, and this repo's own
# `pr-gates` executes the payload on every PR — so a comparator that broke on either side reds
# rather than quietly exempting.
# Revisit if a shared normalization helper is ever hoisted into a file both can reach: a single
# canonical comparator makes an ordinary `verbatim` row possible, and this entry should become
# one.

# lean ARTIFACT-NAME suffixes (#359) — the `*-lean.md` / `*-lean-verdict.md` /
# `*-lean-intent-gap.md` / `*-lean-renders.md` table, held by scripts/check-lean-chain.sh
# (anchor `lean-chain-artifact-patterns`) and, for the three it still reads, by
# lean-evidence.sh's own pinned name table. Both are CI-side readers that cannot see the
# gitignored runtime config lean-gate.sh derives these names from.
# Considered for a row and DROPPED: the two sets are deliberately DIFFERENT, so neither relation
# fits. `-lean-renders.md` belongs only to check-lean-chain.sh (the design-render arm is parked
# outside the consumer core by this issue's OR-1) and `-lean-intent-gap.md` belongs only to
# lean-evidence.sh (the ratification arm moved there in full). `verbatim` would compare unlike
# sets and fail on a correct tree; `subset-of` reads the FIRST quoted literal of each block,
# which here is a lone suffix rather than an enum, so it would compare `-lean.md` against
# `-lean.md` and assert nothing about the rest.
# Guarded behaviorally instead, and end to end rather than by name: check-lean-chain-selftest.sh
# drives a real fixture tree through the delegating boundary, so a suffix that diverged on either
# side stops locating the artifact and reds — the (A) happy path and (S0)-(S4) between them
# exercise every name in both tables.
# Revisit if OR-1 lands: the render suffix would then exist on both sides and the sets converge.
#
# The VERDICT suffix alone DOES get a row (#542), and that is not a reversal of the DROP above.
# What changed is that it acquired a third holder with a different transport: the consumer CI
# delta guard (plugins/second-shift/templates/consumer/second-shift-delta-guard.sh) recognises
# the docs-only verdict commit by this exact suffix, and is COMMITTED INTO a consumer repo
# rather than fetched at the pinned ref — so no behavioral end-to-end run in this tree can
# observe that pair the way check-lean-chain-selftest.sh observes the others. Nor would a
# consumer: a diverged suffix leaves the guard classifying every verdict commit as ordinary,
# the heavy lane runs in full, and the only symptom is the CI bill this guard exists to lower.
# Silent, and green the whole time.
# ONE line inside each block, comments deliberately OUTSIDE the markers, which is what makes
# `verbatim` the honest relation here rather than the unlike-sets comparison the DROP rejects.
lean-verdict-suffix	verbatim	plugins/dev-pipeline/skills/build-lean/lean-evidence.sh	lean-verdict-suffix	plugins/second-shift/templates/consumer/second-shift-delta-guard.sh	lean-verdict-suffix

# lean evidence TOKEN SCOPES (#359) — the `permissions:` block granting `contents` + `issues` +
# `pull-requests` read, held by this repo's `.github/workflows/ci.yml` (`pr-gates`) and by
# `plugins/second-shift/templates/consumer/second-shift-ci.yml`. Both jobs execute the same
# lean-evidence identity arm, whose `GET /repos/{o}/{r}/issues/{n}/comments` read is denied
# without them — and a `permissions:` key replaces the workflow defaults wholesale, so an
# omitted scope is `none` rather than inherited. The template shipped without the two reads and
# would have red-lined every adopting consumer's lean PRs with an environment error.
# Considered for a row and DROPPED, and NOT for want of an anchor — the two blocks do collapse
# to the same string today. They are not one contract. The host job additionally runs
# check-lean-chain.sh's issue-side claim arm, which a read-only tracker has no counterpart for,
# so the sets coincide by present need, not by definition. A `verbatim` row would bind them in
# the wrong direction: a scope the HOST later needs would become a scope every consumer's
# workflow is forced to grant, which is precisely the standing escalation the template's own
# comment refuses. The correct relation is "each block ⊇ what its own arms read", which no
# relation in this file expresses.
# Guarded asymmetrically instead, because the two sides have unequal signals. The template has
# NO live signal — no consumer CI runs in this repo — so second-shift-ci-check-selftest.sh pins
# all three scopes against the block itself (not the file, so a commented-out scope cannot
# satisfy it) plus the no-write-scope rule. The host side has the strongest signal available:
# its own pr-gates job executes that read on every PR, so a dropped scope reds immediately and
# visibly. An assertion there would restate what CI already proves.
# Revisit if the host's block gains a scope: check whether the arm needing it lives in the
# payload (then the template needs it too) or only in check-lean-chain.sh (then it must not).
#
# config-grill.sh's restated RUNTIME-resolved defaults vs the stages that resolve them.
# The checker's trigger-2 table quotes, for each glob key, the jq fallback literal the
# consuming stage actually applies — `apps/web/**/*.{tsx,jsx}` for stageParams.webComponentGlobs
# and `*.{ts,tsx,js,json,md}` for stageParams.formatGlob. It carried a third restatement, the
# four shipped render-surface globs for stageParams.visualCapture.triggerGlobs; that row is
# deleted outright — visual capture is not a default-lane capability, so no key under it is
# evaluated any more and there is nothing left to keep in step. Quoting the SCHEMA default is a lie
# the consumer cannot act on: nothing injects schema defaults into a config, so the value that
# takes effect is the fallback in the stage. That makes the restatement load-bearing, and a
# stage that changes its fallback while the checker keeps the old one produces a finding whose
# "resolved default" names a literal no longer in force.
# Considered for rows and DROPPED. `webComponentGlobs`'s literal alone is restated at seven
# sites (review-lead/SKILL.md x5, plus the config-grill and schema restatements), and this
# file's relations are file-to-file pairs with one in-file anchor per side — a pair cannot
# express one canonical against seven scattered restatements, and picking one arbitrary site
# as the canonical would leave the other six free to drift while the row stayed green. Nor is
# a marker block placeable around them: they sit inside prose sentences and a jq expression,
# not around a declared literal. CLAUDE.md sanctions exactly this case — record a real but
# unanchorable coupling as DROPPED so the decision is visible rather than forgotten.
# Guarded instead by config-grill-selftest.sh, which asserts each default fires a zero-match
# finding on a fixture tree that deliberately contains no matching path, so the literal is
# exercised rather than merely present. That is a check on the checker's copy, not on the two
# staying equal — the asymmetry is the point of this note.
# Revisit if the fallbacks are ever hoisted into one shared resolver: a single canonical site
# makes an ordinary `verbatim` row possible, and this entry should become one.
#
# onboard/SKILL.md's benefit clauses vs the docs that own the worked examples.
# Each of the five already-elicited capability questions (gates/mutation, design + liveRender,
# reviewer deltas, the review-context scaffold, the CI workflows) now states in ONE clause what
# the consumer gets, and points at the authority: docs/extending.md §3.3 / §3.5,
# docs/config-schema.md's `gates` row, docs/extension-points.md "Authoring the review-context
# surface", docs/team-rollout.md. The coupling is real — a capability whose behavior changes
# leaves a clause promising something the tool no longer does, and onboard is precisely where a
# human decides on that promise.
# Considered for rows and DROPPED. There is no literal on either side to anchor: the clause is a
# summary IN a sentence and the authority is a multi-paragraph worked example, so `verbatim` has
# nothing to compare and `subset-of` would compare a paraphrase against prose it deliberately
# does not quote. Wrapping markers around them would pin only that some text exists between the
# markers. CLAUDE.md forbids the alternative outright — grepping a literal out of a markdown file
# asserts that prose contains words and cannot fail for a reason a reader of the diff would not
# already see — so this is recorded as DROPPED rather than mechanized into a guard that cannot
# fail. What holds it instead: the clause is a POINTER plus one sentence, deliberately short
# enough that the authoritative text stays in exactly one place, so drift has little surface to
# occur on and a reviewer reading the capability's diff sees the pointer.
# Revisit if the benefit text is ever hoisted into a data file both sites read: a single
# canonical site makes an ordinary `verbatim` row possible, and this entry should become one.
#
# The monorepo probe. Two shipped selftests assert a lockstep against an artifact that lives
# in this repo and ships inside no plugin (schema/second-shift.config.schema.json,
# docs/extension-points.md). Each probes whether it is running in the monorepo to decide
# between a hard FAIL and a counted rc-77 skip, and each carries its own copy of the marker
# test because the two live in different plugins at different up-counts. The differing `ROOT=`
# assignment sits ABOVE the markers on purpose, so only the shared test is inside the block and
# a plain `verbatim` row is expressible — a widened or renamed marker on one side alone is
# exactly the drift that would let one suite skip where the other still fails.
monorepo-probe	verbatim	plugins/dev-pipeline/tools/config-lint-selftest.sh	monorepo-probe	plugins/review-toolkit/scripts/check-review-context-sections-selftest.sh	monorepo-probe
#
# The dup-scan exit taxonomy. Considered for rows and DROPPED. `dup-scan.sh`'s 0 / 10 / 2
# contract is restated in four SKILL blocks — intake-orchestrator twice (the Step 5.5 parent
# scan and the Step 6 per-slice scan), intake-interviewer, plan-interview — and nothing couples
# them to the tool. There is no shared literal to anchor: each site states the obligation in the
# vocabulary of its own exit (what "hard-stop" means differs per exit — do not label, do not
# hand off, do not create), so a `verbatim` block would either force four prose passages into
# one wording they do not share or degrade to a bare-number grep that CLAUDE.md forbids as a
# prose-presence guard. What holds it instead: the numbers themselves are pinned on the tool
# side by dup-scan-selftest.sh, which asserts each arm's rc AND the message it names, so a
# taxonomy change reds there before any SKILL copy can be silently wrong about it. The SKILL
# copies can still drift into describing an arm the tool no longer has; that drift is visible
# in the diff of any change to the taxonomy, which necessarily touches the tool and its suite.
# Revisit if the obligations are ever hoisted into one shared block the exits include by
# reference: a single canonical site makes an ordinary `verbatim` row possible.
#
# The three-outcome checked-call idiom. `checked_match` has consumers in TWO plugins, and a
# sibling `source` across a plugin boundary is a path resolved by hop count — the trap #469 was
# filed for, which breaks under the version-keyed install cache. So dev-pipeline SOURCES the
# canonical file and second-shift's detect.sh carries an inline copy, byte-identical.
#
# `verbatim` over the WHOLE block, comments included: the comments are what tell the next
# reader that rc 2 means UNKNOWN and must not be folded into a caller's `else`. A copy that
# kept the code and dropped that paragraph would be the fail-open this idiom exists to remove,
# reintroduced one call site at a time.
checked-call	verbatim	plugins/dev-pipeline/tools/checked-call.sh	checked-call	plugins/second-shift/skills/onboard/tools/detect.sh	checked-call
#
# The reserved verify-lane INFRASTRUCTURE exit code (#527), writer ↔ reader. tools/run-selftests.sh
# RAISES 3 when every failing suite is its no-verdict class; lean-gate.sh milestone 3 READS 3 from
# any verify lane as "nothing was evaluated" and charges no fix attempt. Considered for a row and
# DROPPED, for LEAN_JOB_CEILING's reason one field over: the two sites share a NUMBER, not an
# anchorable block, and pinning a literal in two places is the presence check this repo bans.
#
# It is not left reviewer-guarded either, which is where this differs from the ceiling above.
# lean-gate-selftest.sh (ic6)/(ic7) COMPOSE the pair — the real runner, over a fixture tree whose
# every suite dies without a verdict, wired into `commands.acme.test` exactly as a consumer would
# wire it — so a one-sided change of the number reds there in both polarities. The two ends are
# additionally pinned alone: run-selftests-selftest.sh's AC-1 cases on the writer, (ic1)-(ic5) on
# the reader. The composed case is repo-only by construction (the staged install cache has no
# `tools/`), and says so rather than skipping in silence.
#
# iso_to_epoch pair (#565). retro-corpus.sh's `timing` mode needs ISO-8601 -> epoch and cannot
# import pipeline-cost-block.sh's copy: #546 owns every executable line of that file, so
# extracting a shared helper would collide with it head-on. The contract is the BSD/GNU dual
# form WITH `-u` on the BSD arm — without it `date -j -f` reads a `Z` string as local time and
# neither wrong form fails cleanly, so drift here is a silent offset rather than a crash.
#
iso-to-epoch	verbatim	plugins/dev-pipeline/tools/pipeline-cost-block.sh	iso-to-epoch	plugins/dev-pipeline/tools/retro-corpus.sh	iso-to-epoch
